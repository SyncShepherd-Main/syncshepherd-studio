{
  "name": "pagecast-fetcher",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker fetch proxy for PageCast",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
